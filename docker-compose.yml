services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: pubtrack
      POSTGRES_PASSWORD: pubtrack
      POSTGRES_DB: pubtrack
    ports:
      - '5432:5432'
    volumes:
      - pubtrack_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U pubtrack -d pubtrack']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pubtrack_pgdata:
