services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: pubtrack
      POSTGRES_PASSWORD: pubtrack
      POSTGRES_DB: pubtrack_test
    ports:
      - '5433:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U pubtrack -d pubtrack_test']
      interval: 5s
      timeout: 5s
      retries: 10
  redis:
    image: redis:7-alpine
    ports:
      - '6380:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 10
